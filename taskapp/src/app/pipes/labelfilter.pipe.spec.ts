import { LabelfilterPipe } from './labelfilter.pipe';

describe('LabelfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
